spring:
  application:
    name: anime-service
  threads:
    virtual:
      enabled: true
logging:
  level:
    academy.devdojo: debug

server:
  servlet:
    context-path: /
  error:
    include-stacktrace: on_param
    include-message: always
  tomcat:
    threads:
      max: 20